public interface SideJack {
    String task = "Lift the car in the air from behind or operate the side jack";
    public void liftSides(RaceCar car);
    
    // Método adicional
    public void adjustPosition(String direction);
}
